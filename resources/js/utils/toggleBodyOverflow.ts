
/**
 * This function toggles the body overflow. It is used to prevent the user 
 * from scrolling the page while the modal is open.
 * 
 */
export default function toggleBodyOverflow(){
    document.body.classList.toggle("overflow-hidden");
}