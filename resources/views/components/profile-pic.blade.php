<img src="{{URL::asset("$profile_image")}}"
        alt="{{$alias}}" 
        width="{{$width}}" 
        height="{{$height}}" 
        class="rounded-circle border mx-auto {{$addClasses}}" >

        {{-- {{URL::asset("assets/avatar.svg")}} default asset when there's no pic--}}